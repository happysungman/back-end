server:
  port: 8080

spring:
  application:
    name: backend-study
  profiles:
    active: local
  datasource:
    backend:
      jpa:
        leader:
          pool-name: backend-leader-hikari-pool
          driver-class-name: org.mariadb.jdbc.Driver
          jdbc-url: jdbc:mariadb://localhost:3307/shop?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
          connection-test-query: SELECT 1
          username: root
          password: backend
        follower:
          pool-name: backend-follower-hikari-pool
          driver-class-name: org.mariadb.jdbc.Driver
          jdbc-url: jdbc:mariadb://localhost:3307/shop?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
          connection-test-query: SELECT 1
          username: read_user
          password: readonly
          read-only: true
      r2dbc:
        leader:
          host: localhost
          port: 3307
          database: shop
          username: root
          password: backend
        follower:
          host: localhost
          port: 3307
          database: shop
          username: read_user
          password: readonly
  jpa:
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
    show-sql: true
#    database: MYSQL
    database-platform: org.hibernate.dialect.MariaDBDialect